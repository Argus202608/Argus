/** Canonical Argus desktop gateway API.
 *
 * The implementation remains in `hermes.ts` as a compatibility boundary for
 * older extensions; new renderer code should import this module.
 */
export * from './hermes'
